{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate --schema=src/prisma/schema.prisma && npm run build --workspace=apps/api"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=apps/api/src/prisma/schema.prisma && node apps/api/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
